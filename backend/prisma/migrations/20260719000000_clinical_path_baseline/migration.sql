-- SoulCap clinical path baseline
-- Full schema lives in schema.prisma. Apply with:
--   npx prisma migrate deploy
-- or for greenfield lab:
--   npx prisma db push
-- This migration records the clinical-path release point (v0.2.0).
SELECT 1;
