# SPEC v2.3 — Values path, opposite-action helper, body-scan entry

> **Later wave** after v2.2. Regulation depth without therapy branding.
> Inherit `AGENTS.md`. Capacity-first when Wired. Trauma-aware filters stay on.

---

## 1. Values / ACT-flavoured mini-path

Entry: from Guided Path result when family is `activation` or chip `avoid`, or Calm Also here.

Steps:

1. Pick up to 2 values chips: connection · rest · usefulness · kindness · creativity · honesty  
2. “One tiny action that fits” — free text (≤80 chars) or pick from BA skill  
3. Save as Manual line or pending BA run  

Copy: committed action language, not “ACT therapy plan”. Footnote OK: ideas related to ACT education.

---

## 2. Opposite-action helper

Sheet:

1. Urge chip: withdraw · lash out · freeze · hide  
2. Suggest opposite micro-move (maps to `opposite-action` + custom one-liners in `data.js`)  
3. Begin skill or “I’ll try my own version” → journal stub  

Educational DBT footnote only.

---

## 3. Body-scan entry

- Calm / path secondary: launch `body-scan` with existing runner  
- Honour `prefers-reduced-motion` and traumaCaution (skip auto-suggest if trauma marked; still browsable with caution)

---

## 4. Capacity gate (shared)

Before cognitive packs when arrival is Wired: ask “How much room do you have?” Low → prefer autonomic/sensory/orienting. Reuse `capacityFits`.

---

## 5. Tests

- Values path saves without diagnosis words  
- Opposite-action opens skill  
- Body-scan runner; traumaCaution respected in auto path  

## 6. Ship

e.g. 2.3.0 / `soulcap-v230` after v2.2.
